{
  "name": "esf-pdf-service",
  "version": "1.0.0",
  "description": "service for generating PDF files from JSON",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Timur",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.6.12",
    "express": "^4.14.0",
    "phantom": "^3.0.1"
  },
  "devDependencies": {
    "mocha": "^3.1.2"
  }
}
